# Vendor Directory

This directory is reserved for pinned source snapshots required by the native
plugin build.

The intended production input is a PGlite Postgres source snapshot. The snapshot
should be imported without local build artifacts and with enough provenance to
reproduce the native link manifest.

