[package]
name = "libpglite-native"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
publish = false
build = "build.rs"

[lib]
path = "src/lib.rs"
doctest = false

[features]
internal-adapter = []

[dependencies]
libpglite = { path = ".." }

